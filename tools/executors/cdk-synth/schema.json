{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "main": {
      "type": "string",
      "description": "The file path to main.js, which should be a TS compiled CDK Stack (microservice)."
    },
    "outputPath": {
      "type": "string",
      "description": "Emits the synthesized cloud assembly into a directory."
    }
  }
}
